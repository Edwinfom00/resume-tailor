export type Messages = {
  metadata: {
    title: string;
    description: string;
  };
  languageSwitcher: {
    label: string;
  };
  home: {
    eyebrow: string;
    title: string;
    description: string;
    primaryAction: string;
    secondaryAction: string;
  };
};
